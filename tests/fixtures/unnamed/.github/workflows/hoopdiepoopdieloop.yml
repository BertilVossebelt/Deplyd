name: Hoopdiepoopdieloop
on:
  workflow_dispatch:
jobs:
  ship-it:
    runs-on: ubuntu-latest
    environment:
      name: production
    defaults:
      run:
        working-directory: src/api
    steps:
      - uses: actions/checkout@v4
      - name: Publish
        run: echo x
