name: Deploy production
on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
      - uses: ./.github/actions/prepare
      - name: Build
        run: echo build
      - name: Ship
        run: echo ship
