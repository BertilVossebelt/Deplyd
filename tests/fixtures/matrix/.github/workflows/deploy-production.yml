name: Deploy production
on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
    strategy:
      matrix:
        service: [api, web]
    defaults:
      run:
        working-directory: services
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo build
      - name: Ship
        run: echo ship
